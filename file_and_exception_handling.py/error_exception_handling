"""
1.compile time error=>syntax error & indentation error
2.Exception=>error during execution.
"""
"""
x=10
result=x+y 
here we get an eception like y is not define so this is one of error.
"""

#how to handle exception=>using a try-except block.
try:
    num1=int(input("enter a number: "))
    num2=int(input("enter another number"))
    result=num1/num2
    print(result)
except ZeroDivisionError:
    print("the denominator cannot be zero")
except ValueError:
    print("input should only be digits")
